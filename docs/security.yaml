openapi: 3.0.0
components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: |
        **JWT Authentication** 

        Authentication using the Bearer scheme

        `Authorization: JWT eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`
security:
  - BearerAuth: []
